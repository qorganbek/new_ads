#include <bits/stdc++.h>

using namespace std;

int main (){
    map<pair<string, string>, pair<int, double> > m;
    int n;
    cin >> n;
    for (int i = 0; i < n; i++){
        string name,sur_name;
        int num;
        cin >> name >> sur_name >> num;
        for (int j = 0; j < num; j++){
            int credit;
            char gpa;
            cin >> gpa >> credit;
        } 
    }
    return 0;
}